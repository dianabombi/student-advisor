# 📊 Як Використовувати Analytics Dashboard

## Швидкий Старт

### Крок 1: Відкрийте Дашборд

Просто відкрийте файл `analytics_dashboard.html` у вашому браузері:

**Windows:**
```
Подвійний клік на файл: backend\analytics_dashboard.html
```

Або перетягніть файл у браузер (Chrome, Firefox, Edge).

### Крок 2: Введіть Дані для Входу

1. **API URL**: `http://localhost:8001` (якщо бекенд запущений локально)
2. **Токен авторизації**: Ваш токен (отримайте після входу на платформі)

### Крок 3: Натисніть "Увійти"

Дашборд автоматично завантажить всі дані та покаже графіки!

## Як Отримати Токен?

### Варіант 1: Через Браузер (DevTools)

1. Відкрийте `http://localhost:3001` (фронтенд CODEX)
2. Залогіньтесь
3. Натисніть `F12` (відкрити DevTools)
4. Перейдіть на вкладку **Application** → **Local Storage** → `http://localhost:3001`
5. Знайдіть ключ `token` або `auth_token`
6. Скопіюйте значення

### Варіант 2: Через API

```bash
curl -X POST "http://localhost:8001/api/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=ваш_email@example.com&password=ваш_пароль"
```

Відповідь містить `access_token` - це ваш токен.

### Варіант 3: Зробіть Себе Admin

Якщо ви власник платформи, зробіть себе admin в базі даних:

```sql
-- Підключіться до PostgreSQL
docker exec -it codex-db-1 psql -U user -d codex_db

-- Зробіть себе admin
UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';

-- Вийдіть
\q
```

Потім залогіньтесь на платформі та отримайте токен.

## Що Показує Дашборд?

### 📊 Вкладка "Огляд"

**Статистика:**
- Унікальні відвідувачі
- Перегляди сторінок
- Реєстрації
- Активні підписки
- Загальний виторг

**Графіки:**
- Відвідувачі та реєстрації по днях (лінійний графік)
- Воронка конверсії (стовпчиковий графік)

### 👥 Вкладка "Відвідувачі"

**Графіки:**
- Джерела трафіку (кругова діаграма)
  - Google, Facebook, Direct, тощо
- Пристрої (кругова діаграма)
  - Mobile, Desktop, Tablet

### 💰 Вкладка "Виторг"

**Графіки:**
- Виторг по днях (стовпчиковий графік)
- Підписки по тарифах (стовпчиковий графік)
  - 1 місяць, 6 місяців, 1 рік

### 📈 Вкладка "Маркетинг"

**Статистика:**
- CAC (вартість залучення клієнта)
- Середній LTV (Lifetime Value)
- LTV:CAC Ratio (має бути > 3)
- Витрати на маркетинг

**Таблиці:**
- **ROI по каналах**
  - Канал, Витрати, Виторг, Прибуток, ROI, Конверсія
- **Ефективність кампаній**
  - Назва кампанії, Канал, Відвідувачі, Конверсії, ROI, Статус

## Функції Дашборду

### Вибір Періоду

У верхній частині дашборду є селектор періоду:
- Останні 7 днів
- Останні 30 днів (за замовчуванням)
- Останні 90 днів

Виберіть період і дані автоматично оновляться.

### Перемикання Вкладок

Натискайте на вкладки для перегляду різних розділів:
- 📊 Огляд
- 👥 Відвідувачі
- 💰 Виторг
- 📈 Маркетинг

### Інтерактивні Графіки

Всі графіки інтерактивні:
- **Наведіть курсор** на точку/стовпчик - побачите деталі
- **Клікніть на легенду** - приховати/показати дані

## Приклад Використання

### Сценарій 1: Щоденний Моніторинг

1. Відкрийте дашборд вранці
2. Подивіться на вкладку "Огляд"
3. Перевірте скільки нових відвідувачів та реєстрацій за вчора
4. Перейдіть на "Виторг" - подивіться чи були нові платежі

### Сценарій 2: Аналіз Маркетингу

1. Перейдіть на вкладку "Маркетинг"
2. Подивіться на CAC - чи не занадто високий?
3. Перевірте LTV:CAC Ratio - чи > 3?
4. Подивіться таблицю ROI - які канали працюють найкраще?
5. Зупиніть кампанії з негативним ROI

### Сценарій 3: Оптимізація Трафіку

1. Перейдіть на вкладку "Відвідувачі"
2. Подивіться графік "Джерела трафіку"
3. Знайдіть найбільше джерело
4. Інвестуйте більше в це джерело

## Технічні Деталі

### Технології

- **HTML5** - структура
- **CSS3** - стилі (градієнти, тіні, анімації)
- **JavaScript** - логіка
- **Chart.js 4.4.0** - графіки

### Безпека

- Токен зберігається тільки в пам'яті браузера
- При закритті вкладки токен видаляється
- Всі запити йдуть через HTTPS (якщо API на HTTPS)

### Оновлення Даних

Дані оновлюються:
- При вході
- При зміні періоду
- При перемиканні вкладок (якщо потрібно)

Для ручного оновлення - просто змініть період і поверніть назад.

## Поради

1. **Додайте в закладки** - щоб швидко відкривати
2. **Використовуйте на великому екрані** - графіки краще видно
3. **Перевіряйте щодня** - слідкуйте за трендами
4. **Експортуйте дані** - використовуйте API для експорту в CSV

## Troubleshooting

### Помилка "Невірний токен"

- Перевірте що ви admin (`role = 'admin'` в базі даних)
- Перевірте що токен не застарів (токени діють 30 хвилин)
- Отримайте новий токен

### Помилка "Connection refused"

- Перевірте що бекенд запущений: `docker-compose ps`
- Перевірте URL: має бути `http://localhost:8001`

### Графіки не показуються

- Перевірте консоль браузера (F12 → Console)
- Можливо немає даних за вибраний період
- Спробуйте вибрати більший період (90 днів)

### Маркетингова аналітика не показується

- Це нормально якщо ви ще не створили жодної кампанії
- Створіть кампанію через API або додайте вручну в базу даних

## Наступні Кроки

Після того як звикнете до дашборду:

1. **Налаштуйте автоматичні звіти** - використовуйте API для генерації PDF звітів
2. **Додайте алерти** - отримуйте сповіщення при падінні метрик
3. **Інтегруйте з іншими системами** - Google Analytics, Facebook Pixel, тощо

---

**Файл дашборду:** `backend/analytics_dashboard.html`

**Підтримка:** Якщо виникли питання, перевірте логи бекенду: `docker-compose logs backend`
