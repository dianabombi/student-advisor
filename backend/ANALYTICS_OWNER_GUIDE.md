# 📊 Аналітика Платформи CODEX - Інструкція для Власника

## Що Це Таке?

Система аналітики дозволяє вам, як власнику платформи, відстежувати всі ключові метрики вашого бізнесу:
- 👥 Скільки людей відвідує сайт
- 📝 Скільки реєструється нових користувачів
- 💳 Які тарифи обирають
- 💰 Скільки грошей заробляє платформа
- 🔍 Звідки приходять відвідувачі

## Як Отримати Доступ?

### 1. Зробіть Себе Адміністратором

Щоб отримати доступ до аналітики, ваш обліковий запис повинен мати роль `admin`.

**Як це зробити:**

1. Відкрийте базу даних PostgreSQL
2. Знайдіть свого користувача в таблиці `users`
3. Змініть поле `role` на `'admin'`

```sql
-- Приклад SQL команди
UPDATE users 
SET role = 'admin' 
WHERE email = 'ваш_email@example.com';
```

### 2. Отримайте Токен Авторизації

Після входу в систему, ваш токен автоматично зберігається в браузері. Для API запитів використовуйте цей токен в заголовку:

```
Authorization: Bearer ВАШ_ТОКЕН
```

## API Ендпоінти

Всі ендпоінти знаходяться за адресою: `http://localhost:8001/api/owner/analytics/`

### 📊 Головний Дашборд

**GET** `/api/owner/analytics/dashboard?days=30`

Повертає всю статистику за останні N днів (за замовчуванням 30).

**Приклад відповіді:**
```json
{
  "period": {
    "start_date": "2025-11-21T13:00:00",
    "end_date": "2025-12-21T13:00:00",
    "days": 30
  },
  "summary": {
    "unique_visitors": 1250,
    "total_page_views": 5430,
    "registrations": 87,
    "active_subscriptions": 23,
    "total_revenue": 1840
  },
  "traffic_sources": [...],
  "device_statistics": {...},
  "subscription_statistics": {...},
  "revenue_statistics": {...},
  "conversion_funnel": {...}
}
```

### 👥 Аналітика Відвідувачів

**GET** `/api/owner/analytics/visitors?days=30`

Детальна інформація про відвідувачів.

**Що показує:**
- Графік відвідувачів по днях
- Звідки приходять (Google, Facebook, direct)
- Які пристрої використовують (mobile, desktop, tablet)
- Які браузери та ОС
- Список останніх 50 відвідувачів

### 👤 Аналітика Користувачів

**GET** `/api/owner/analytics/users?days=30`

Інформація про реєстрації.

**Що показує:**
- Графік реєстрацій по днях
- Список останніх 100 зареєстрованих користувачів
- IP адреси та User Agent
- Статус підписки кожного користувача

### 💳 Аналітика Підписок

**GET** `/api/owner/analytics/subscriptions?days=30`

Статистика по тарифах.

**Що показує:**
- Скільки людей вибрали кожен тариф
- Конверсія з trial в платну підписку
- Графік підписок по днях
- Список останніх 100 підписок

### 💰 Аналітика Виторгу

**GET** `/api/owner/analytics/revenue?days=30`

Детальна інформація про доходи.

**Що показує:**
- Загальний виторг
- Виторг по тарифах
- Графік виторгу по днях
- Виторг по місяцях (останні 12 місяців)
- Середній чек
- Список останніх 100 платежів

### 📈 Графіки

**GET** `/api/owner/analytics/charts/visitors?days=30`
**GET** `/api/owner/analytics/charts/registrations?days=30`
**GET** `/api/owner/analytics/charts/revenue?days=30`

Окремі ендпоінти для отримання даних для графіків.

### 🎯 Воронка Конверсії

**GET** `/api/owner/analytics/funnel?days=30`

Показує як відвідувачі конвертуються в платних користувачів:
1. Відвідувачі → Реєстрації
2. Реєстрації → Trial
3. Trial → Платні підписки

### 📥 Експорт в CSV

**GET** `/api/owner/analytics/export/csv?report_type=users&days=30`

Експортує дані в CSV файл.

**Типи звітів:**
- `users` - список користувачів
- `subscriptions` - список підписок
- `payments` - список платежів
- `visitors` - список відвідувачів

## Приклади Використання

### Приклад 1: Отримати Дашборд через cURL

```bash
curl -X GET "http://localhost:8001/api/owner/analytics/dashboard?days=30" \
  -H "Authorization: Bearer ВАШ_ТОКЕН"
```

### Приклад 2: Отримати Статистику Відвідувачів

```bash
curl -X GET "http://localhost:8001/api/owner/analytics/visitors?days=7" \
  -H "Authorization: Bearer ВАШ_ТОКЕН"
```

### Приклад 3: Експортувати Користувачів в CSV

```bash
curl -X GET "http://localhost:8001/api/owner/analytics/export/csv?report_type=users&days=30" \
  -H "Authorization: Bearer ВАШ_ТОКЕН" \
  -o users_report.csv
```

### Приклад 4: Використання в Python

```python
import requests

# Ваш токен авторизації
token = "ВАШ_ТОКЕН"
headers = {"Authorization": f"Bearer {token}"}

# Отримати дашборд
response = requests.get(
    "http://localhost:8001/api/owner/analytics/dashboard?days=30",
    headers=headers
)

data = response.json()
print(f"Унікальних відвідувачів: {data['summary']['unique_visitors']}")
print(f"Реєстрацій: {data['summary']['registrations']}")
print(f"Виторг: {data['summary']['total_revenue']} EUR")
```

## Що Відстежується Автоматично?

### При Відвідуванні Сайту:
- IP адреса
- User Agent (браузер, пристрій, ОС)
- Referrer (звідки прийшов)
- UTM параметри (якщо є)
- Час візиту

### При Реєстрації:
- Всі дані з відвідування
- Email та ім'я
- IP адреса
- User Agent
- Час реєстрації

### При Підписці:
- Який тариф вибрав
- Сума
- Дата початку та закінчення
- Статус (trial, active, expired)

### При Платежі:
- Сума
- Метод оплати
- Transaction ID
- Статус

## Як Додати UTM Параметри?

UTM параметри дозволяють відстежувати звідки приходять користувачі.

**Приклад посилання з UTM:**
```
https://yourcodex.com/?utm_source=facebook&utm_medium=cpc&utm_campaign=winter2025
```

**Параметри:**
- `utm_source` - джерело (google, facebook, instagram)
- `utm_medium` - тип трафіку (cpc, email, social)
- `utm_campaign` - назва кампанії (winter2025, promo)

## Поради

1. **Регулярно Перевіряйте Дашборд** - щотижня дивіться на ключові метрики
2. **Аналізуйте Джерела Трафіку** - зрозумійте звідки приходять найкращі користувачі
3. **Відстежуйте Конверсію** - дивіться скільки trial користувачів стають платними
4. **Експортуйте Дані** - регулярно зберігайте CSV звіти для аналізу
5. **Використовуйте UTM** - додавайте UTM параметри до всіх рекламних посилань

## Файли Системи Аналітики

Всі файли знаходяться в папці `backend`:

1. **`api/platform_owner_analytics.py`** - головний файл з API ендпоінтами
2. **`services/analytics_service.py`** - сервіс для обробки даних
3. **`middleware/analytics_middleware.py`** - автоматичне відстеження
4. **`main.py`** - моделі бази даних (VisitorTracking, PageView)

## Підтримка

Якщо виникли питання або проблеми:
1. Перевірте що ваш обліковий запис має роль `admin`
2. Перевірте що токен авторизації дійсний
3. Перевірте логи бекенду: `docker-compose logs backend`

---

**Створено для платформи CODEX**
**Версія: 1.0**
**Дата: 21.12.2025**
